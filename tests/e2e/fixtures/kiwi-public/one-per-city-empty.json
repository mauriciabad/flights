{
	"extensions": {
		"note": "FIXTURE — a mocked Kiwi.com response, not a real provider answer. GraphQL allows any top-level `extensions` object, so the marker guard.spec.ts requires lives here rather than in a field the adapter reads."
	},
	"data": {
		"onewayOnePerCityItineraries": {
			"__typename": "OnePerCityItineraries",
			"itineraries": []
		}
	}
}
